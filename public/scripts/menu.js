const menutItem = function(item) {`
<section class="menu-item">
    <img src="${item.picture_url}" alt=" pasta bake"/>
    <p class ="item-description">${item.description}</p>
    <p class="price"> $${item.price}</p>
    <button class="add-to-order">Add</button>
  </div>
`}
